<UserControl
    x:Class="YourNoteUWP.NoteContent"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:data="using:YourNoteUWP.Models"
    xmlns:local="using:YourNoteUWP"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="300"
    d:DesignWidth="400"
    mc:Ignorable="d">


    <UserControl.Resources>

        <DataTemplate x:Key="shareUser" x:DataType="data:User">
            <StackPanel HorizontalAlignment="Center" Orientation="Horizontal">
                <StackPanel Margin="10">
                    <TextBlock
                        HorizontalAlignment="Left"
                        FontSize="25"
                        Foreground="Red"
                        Text="{x:Bind userId}" />
                </StackPanel>
            </StackPanel>
        </DataTemplate>



    </UserControl.Resources>





    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Grid Grid.Column="0" Background="{ThemeResource bgAcrylicBrushHome}" />
        <Grid Grid.Column="2" Background="{ThemeResource bgAcrylicBrushHome}" />
        <Grid Grid.Column="1" Background="#e8b01c">

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <RelativePanel>
                <TextBox
                    Name="Box"
                    Width="700"
                    Height="50"
                    Margin="10"
                    Padding="0,10,0,0"
                    Background="Transparent"
                    BorderBrush="Black"
                    BorderThickness="0,0,0,1"
                    FontSize="28"
                    Foreground="Black"
                    HorizontalTextAlignment="Center"
                    PlaceholderText="Title"
                    RelativePanel.AlignHorizontalCenterWithPanel="True"
                    Style="{StaticResource NoteContentStyle}" />

                <Button
                    Name="NoteCloseButton"
                    Margin="10"
                    Background="Transparent"
                    Click="NoteCloseButton_Click"
                    Content="&#xE106;"
                    FontFamily="Segoe MDL2 Assets"
                    FontSize="16"
                    Foreground="Black"
                    RelativePanel.AlignRightWithPanel="True" />
            </RelativePanel>

            <ListBox
                Name="NoteMenuOptions"
                Grid.Row="1"
                HorizontalAlignment="Center"
                Background="Transparent"
                Style="{StaticResource NoteMenuOptionsStyle}">

                <ListBoxItem>
                    <Button
                        Background="Transparent"
                        Content="&#xE1A2;"
                        FontFamily="Segoe MDL2 Assets"
                        FontSize="16"
                        Foreground="Black" />
                </ListBoxItem>


                <ListBoxItem>
                    <Button
                        Name="NoteShareButton"
                        Background="Transparent"
                        Click="{x:Bind _accountPageViewModel.NoteShareButtonClick, Mode=OneWay}"
                        Content="&#xE72D;"
                        FontFamily="Segoe MDL2 Assets"
                        FontSize="16"
                        Foreground="Black">
                        <Button.Flyout>
                            <Flyout>
                                <Grid>
                                    <ListView
                                        Name="usersToShareView"
                                        Width="350"
                                        Height="400"
                                        Margin="20"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center"
                                        Background="Beige"
                                        BorderBrush="Beige"
                                        BorderThickness="10"
                                        CornerRadius="15"
                                        IsItemClickEnabled="True"
                                        ItemClick="usersToShare_ItemClick"
                                        ItemTemplate="{StaticResource shareUser}"
                                        ItemsSource="{x:Bind usersToShare}" />
                                </Grid>
                            </Flyout>
                        </Button.Flyout>
                    </Button>
                </ListBoxItem>

                <ListBoxItem>
                    <Button
                        Name="NoteDeleteButton"
                        Background="Transparent"
                        Click="{x:Bind _accountPageViewModel.NoteDeleteButtonClick, Mode=OneWay}"
                        Content="&#xE107;"
                        FontFamily="Segoe MDL2 Assets"
                        FontSize="16"
                        Foreground="Black" />
                </ListBoxItem>

            </ListBox>


            <TextBox
                Name="ContentOfNote"
                Grid.Row="2"
                Width="1000"
                Height="750"
                Margin="20,10,20,10"
                Padding="10"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                BorderThickness="0"
                FontSize="20"
                Foreground="Black"
                IsTapEnabled="True"
                Style="{StaticResource NoteContentStyle}"
                Tapped="{x:Bind _accountPageViewModel.ContentOfNoteTapped, Mode=OneWay}"
                Text="asda"
                TextWrapping="Wrap" />

        </Grid>
    </Grid>


</UserControl>
